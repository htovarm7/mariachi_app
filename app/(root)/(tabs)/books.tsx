import { SafeAreaView } from "react-native-safe-area-context";
import { Text } from "react-native";

const Books = () => {
  return (
    <SafeAreaView className="flex-1 items-center justify-center">
      <Text>Welcome to the Books Screen!</Text>
    </SafeAreaView>
  );
};

export default Books;
